// Loader.js
import React from 'react';
import './loader.css'; // Optional: Add some CSS for styling

const Loader = () => (
  <div className="loader">
    <div className="spinner"></div>
  </div>
);

export default Loader;
